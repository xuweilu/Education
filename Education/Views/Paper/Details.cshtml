@model Education.Models.Paper

@{
    ViewBag.Title = "Detail";
}

<h2>Detail</h2>

<div>
    <h4>Paper</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Teacher.TrueName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Teacher.TrueName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.EditOn)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EditOn)
        </dd>
        @foreach(var item in Model.Questions)
        {
            <dt>
                @Html.DisplayNameFor(modelItem => item.Content)
            </dt>
            <dd>
                @Html.DisplayFor(modelItem => item.Content)
            </dd>
        }

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "List")
</p>
